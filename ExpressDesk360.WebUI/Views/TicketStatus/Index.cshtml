
@using ExpressDesk360.WebUI.Models.ViewModels.TicketStatus
@model TicketStatusViewModel
@{
    ViewData["Title"] = "Talep Durumları";
}

@{
    ViewData["Breadcrumb"] = new Breadcrum
    {
        BreadcrumbItems = new List<BreadcrumbItem>
        {
            new BreadcrumbItem { Title = "Anasayfa", Path = Url.Action("Index", "Home") },
        },
        PageName = ViewData["Title"]?.ToString() ?? "Sayfa"
    };
}

<link href="~/metronic/plugins/custom/datatables/datatables.bundle.css" rel="stylesheet" />

<div class="card card-flush">
    <div class="card-header align-items-center py-5 gap-2 gap-md-5">
        <div class="card-title">
            <div class="menu menu-sub menu-sub-dropdown mt-2 w-75 w-lg-500px" data-kt-menu="true">
                <div class="px-7 py-5">
                    <div class="fs-5 text-gray-900 fw-bold">Filtrele</div>
                </div>
                <div class="separator border-gray-200"></div>
                <div class="px-7 py-5">
                    
                    <div class="mb-10">
                        <label class="form-label fw-semibold" for="inpt_name">Ad</label>
                        <input id="inpt_name" class="form-control form-control-sm form-control-solid" name="Name" type="text"/>
                    </div>
                    <div class="mb-10">
                        <div class="form-check form-switch form-switch-sm form-check-custom form-check-solid">
                            <input name="IsDeleted" value="false" type="hidden" />
                            <input name="IsDeleted" value="true" type="checkbox" class="form-check-input" />
                            <span class="form-check-label fs-sm">Silinenleri Dahil Et</span>
                        </div>
                    </div>
            
                    <div class="d-flex justify-content-end">
                        <button type="reset" class="btn btn-sm btn-light btn-active-light-primary me-2" data-kt-menu-dismiss="true">Temizle</button>
                        <button type="button" onclick="InitilazeTable(this)" class="btn btn-sm btn-primary" data-kt-menu-dismiss="true">Uygula</button>
                    </div>
                </div>
            </div>
            <button class="btn btn-sm btn-flex btn-light-dark fw-bold" data-kt-menu-trigger="click" data-kt-menu-placement="bottom-start">
                <i class="ki-duotone ki-filter fs-6 text-muted me-1"><span class="path1"></span><span class="path2"></span></i>
                Filtre
            </button>
        </div>
    </div>
    <div class="card-body p-4 pt-0">
        <table id="main_table" class="table align-middle table-row-dashed fs-6 gy-5">
            <thead>
                <tr class="text-start text-gray-500 fw-bold fs-7 text-uppercase gs-0">
                    <th>Ad</th>
                    <th>Açıklama</th>
                    <th>Oluşturulma Tarihi</th>
                    <th>Son Güncelleme Tarihi</th>
                    <th>Durum</th>
                    <th>Silinme Tarihi</th>
                    <th>İşlemler</th>
                </tr>
            </thead>
            <tbody class="fw-semibold text-gray-600">
            </tbody>
        </table>
    </div>
</div>

@section Scripts {
    <script src="~/metronic/plugins/custom/datatables/datatables.bundle.js"></script>

    <script>

        let mainTable;

        $(document).ready(function(){
            InitilazeTable();
        })

        function InitilazeTable(btn) {
            mainTable = DatatableManager.Create({
                serverSide: true,
                tableId: 'main_table',
                path: 'TicketStatus/DatatableServerSide',
                method: 'Post',
                buttonElement: btn,                
                requestData: {
                    filter: {
                        operator: 'base',
                        logic: 'and',
                        filters: [                             
                            {
                                operator: 'contains',
                                field: 'Name',
                                value: $("input[name='Name']").val()
                            }            
                        ]
                    }
                },        
                columns: [
                    { data: 'name' },
                    { data: 'description' },

                    {
                        data: 'createDateUtc',
                        render: function (data) {
                            if(data == null) return '';
                            return moment(data).format('DD.MM.YYYY HH:mm');
                        }
                    },
                    {
                        data: 'updateDateUtc',
                        render: function (data) {
                            if(data == null) return '';
                            return moment(data).format('DD.MM.YYYY HH:mm');
                        }
                    },, pageTable: mainTable }) :
                                HelperService.DeleteButtonTable({requestUrl: 'TicketStatus/Delete', requestData: { "id": rowData.id }, pageTable: mainTable });

                            DatatableManager.AppendRowButtons(td,
                            [
                                HelperService.UpdateButtonTable({
                                    title: 'Talep Durumu Bilgilerini Güncelle',
                                    formGetterUrl: 'TicketStatus/Update',
                                    requestData: {
                                        "id": rowData.id
                                    },
                                    pageTable: mainTable
                                }),
                                deleteHandleButton
                            ]);
                        }
                    }
                ],
                customButtons:
                [  
                    {
                        text: '<span class="dynamic-content"><i class="fa-solid fa-file-circle-plus me-2"></i>Yeni Talep Durumu Ekle</span>',
                        className: 'btn btn-primary mx-2',
                        action: (e_btn) =>
                        {
                            HelperService.InsertModal({
                                title: 'Yeni Talep Durumu Ekle',
                                formGetterUrl: 'TicketStatus/Create',
                                e_btn: e_btn.currentTarget,
                                pageTable: mainTable
                            })
                        }
                    }
                ]
            })
        }
    </script>
}
